#!/bin/bash -e

HERE="$(readlink -f "$(dirname "${BASH_SOURCE[0]}")")"
PYTHONPATH="$PYTHONPATH:$(dirname "$HERE")/share/python3"

# exec python -m pdb -m annotsv.cli "$@"
exec python -m annotsv.cli "$@"
